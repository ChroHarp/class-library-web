{
  "name": "class-library-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:config": "node ./scripts/generateFirebaseConfig.cjs",
    "build": "npm run generate:config && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
